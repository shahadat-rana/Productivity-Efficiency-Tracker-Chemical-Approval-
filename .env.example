# Outbound Mail Server Configuration (Real background SMTP Relaying)
# All secrets must remain secure server-side

SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM_NAME=
SMTP_FROM_EMAIL=

# Resend API Delivery Configuration (Modern prioritized REST delivery channel)
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
